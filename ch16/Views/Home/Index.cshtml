@model IEnumerable<ch16.Models.Order>
@{
    ViewBag.Title = "Home Page";
}

@*<div class="jumbotron" style="background-color:lightyellow" align="center" valign="center">
        <p>
            <img src="~/images/welcome.png" width="200" />
        </p>
        <h1>歡迎光臨</h1>
        <p class="lead"></p>
    </div>*@

<div class="row">    
    <div class="col-md-10" >
        <table>            
            <tr>
                <td>
                    <h4>是否開放外送服務呢?</h4>
                </td>
                <td>
                    <button type="button" class="btn btn-link" style="font-size:large" onclick="location.href='@Url.Action("CreateDel","Delivery")'">
                        <span class="glyphicon glyphicon-ok-circle"></span> 是
                    </button>
                </td>
            </tr>
        </table>
    </div>
</div>

<h2><img src="~/images/order.png" width="50" /><label color: #fff;> 目前訂單</label></h2>
<div class="row">
    <div class="col-xs-12">
        <table class="table table-striped">
            <tr>
                <th>
                    訂單時間
                </th>
                <th>
                    訂購人
                </th>
                <th>
                    取餐方式
                </th>
                <th>
                    訂單進度
                </th>
                <th>

                </th>
                <th></th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.orderdate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.cuserid)
                    </td>
                    <td>
                        <img src="~/images/@item.oway" width="30" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ostatus)
                    </td>
                    <td>
                        @Html.ActionLink("查看詳情","Detail1","Order", new { id = item.orderguid })|
                        @Html.ActionLink("回報", "", new { id = item.oid })
                    </td>
                </tr>
            }

        </table>
    </div>

</div>
